import {Nav,MobileBar} from '@/components/Nav';import {categories} from '@/lib/data';
export default function Shop(){return <><Nav/><section><div className="section-head"><h2>Shop flowers</h2><p className="muted">A fuller catalog can start with safe categories before importing FSN product data: everyday, sympathy, roses, seasonal, plants, and custom.</p></div><div className="grid3">{categories.concat([{tag:'Seasonal',title:'Summer seasonal mix',copy:'Warm seasonal colors with designer-selected fresh stems.',price:'From $75'},{tag:'Roses',title:'Classic rose arrangements',copy:'Simple rose options by color and stem count, with greenery and vase options.',price:'From $85'},{tag:'Plants',title:'Green plants & keepsakes',copy:'A practical sympathy and everyday category for lower-substitution risk.',price:'From $55'}]).map(c=><article className="card" key={c.title}><span className="pill">{c.tag}</span><h3>{c.title}</h3><p className="muted">{c.copy}</p><b>{c.price}</b></article>)}</div></section><MobileBar/></>}
