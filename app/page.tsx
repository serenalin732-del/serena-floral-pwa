import Link from 'next/link';import {Nav,MobileBar} from '@/components/Nav';import {categories} from '@/lib/data';
export default function Home(){return <><Nav/><main><section className="hero"><div><div className="eyebrow">Neighborhood florist • modern ordering</div><h1>Flowers made personal, without making ordering complicated.</h1><p className="muted">A redesigned website/PWA concept for a Pittsburgh neighborhood flower shop: keep the local warmth, add clearer custom orders, and collect customer details before an order gets lost in email.</p><p><Link className="btn" href="/custom">Try the AI bouquet builder</Link> <Link className="btn ghost" href="/dashboard">View staff dashboard</Link></p><p className="muted">Residential delivery • Funeral & condolence • Email + printable order sheet</p></div><div className="hero-card"><div className="bouquet"/><div className="hero-note"><b>Designed for a safe transition</b><br/><span className="muted">Run beside FSN first: collect requests, send clean emails, and print orders while staff keeps the current workflow.</span></div></div></section><section><div className="section-head"><h2>Shop by need</h2><p className="muted">Simple categories that match how customers already order, without over-changing the shop’s Year 1 brand or product structure.</p></div><div className="grid3">{categories.map(c=><article className="card" key={c.title}><span className="pill">{c.tag}</span><h3>{c.title}</h3><p className="muted">{c.copy}</p><b>{c.price}</b></article>)}</div></section></main><Footer/><MobileBar/></>}
function Footer(){return <footer className="footer"><div className="wrap"><div><b>Springdale Floral PWA Prototype</b><p className="muted">Transition-safe: keep FSN/order email live first, add modern custom ordering and data capture in parallel, then evaluate POS later.</p></div><Link className="btn secondary" href="/custom">Start custom order</Link></div></footer>}
